<!-- Generated by scripts/generate-docs.mjs. Edit demos.js / registry.json / JSDoc. -->

# FloatingDialog

**Status:** Stable  
**Category:** overlays  
**Import:** `drawui`  
**Emitted class(es):** `ContentPanel`, `Header`, `PanelContent`, `RibbonBar`

Lightweight floating panel built on `ContentPanel`.

## Import

```js
import { FloatingDialog } from "drawui";
```

## Examples

### FloatingDialog

```js
return new Button("Open FloatingDialog").addClass("secondary").onClick(() => {
    document.querySelectorAll(".FloatingWindow, .Header").forEach((node) => {
      const shell = node.closest(".FloatingWindow") || node.closest(".Rectangle");
      if (shell?.parentElement === document.body) shell.remove();
    });
    const win = new FloatingDialog({ title: "Floating window", icon: "info" });
    win
      .content(new TextBlock("floatingDialog() content"))
      .add(
        new Button("Close")
          .addClass("secondary")
          .onClick(() => win.destroy()),
      )
      .show();
  });
```

<div class="doc-live-demo" data-live-demo="FloatingDialog/floatingdialog" aria-label="Live preview"></div>

[← Back to API index](./index.html)
