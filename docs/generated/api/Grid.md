<!-- Generated by scripts/generate-docs.mjs. Edit demos.js / registry.json / JSDoc. -->

# Grid

**Status:** Stable  
**Category:** layout  
**Import:** `drawui`  
**Emitted class(es):** `Grid`

Grid layout primitive with explicit column or auto-fit helpers.

## Import

```js
import { Grid } from "drawui";
```

## Examples

### Grid

```js
const grid = new Grid();
  grid.setStyle("display", ["grid"]);
  grid.setStyle("gridTemplateColumns", ["repeat(3, 1fr)"]);
  grid.setStyle("gap", ["0.5rem"]);
  ["A", "B", "C"].forEach((label) => {
    grid.add(
      new Card()
        .add(new TextBlock(label))
        .setStyle("padding", ["0.5rem"])
        .setStyle("textAlign", ["center"]),
    );
  });
  return grid;
```

<div class="doc-live-demo" data-live-demo="Grid/grid" aria-label="Live preview"></div>

[← Back to API index](./index.html)
