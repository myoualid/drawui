<!-- Generated by scripts/generate-docs.mjs. Edit demos.js / registry.json / JSDoc. -->

# Toast

**Status:** Stable  
**Category:** panels  
**Import:** `drawui`  
**Emitted class(es):** `Toast`, `Toast--{type}`, `Toast--fade-out`, `Toast-close`, `status-bar`, `status-bar-fill`

Transient notification or progress overlay.

Notification:
```js
new Toast("Changes saved", "success");
```

Progress:
```js
const toast = new Toast();
toast.show("#host", "Exporting…");
toast.update(50, "50%");
toast.hide();
```

_Source: `src/components/feedback/Toast.js`_

## Import

```js
import { Toast } from "drawui";
```

## Examples

### Progress toast

```js
const inlineHost = new Container();
  inlineHost.dom.id = "inline-progress-host";
  inlineHost.setStyle("position", ["relative"]).setStyle("minHeight", ["3rem"]);
  const toast = new Toast();

  return new StackPanel({ isVertical: true })
    .gap("0.75rem")
    .add(
      new Button("Run inline progress").addClass("secondary").onClick(() => {
        toast.show("#inline-progress-host", "Exporting…");
        let pct = 0;
        const timer = setInterval(() => {
          pct += 10;
          toast.update(pct, `${pct}%`);
          if (pct >= 100) {
            clearInterval(timer);
            setTimeout(() => toast.hide(), 400);
          }
        }, 120);
      }),
    )
    .add(inlineHost);
```

<div class="doc-live-demo" data-live-demo="Toast/progress-toast" aria-label="Live preview"></div>

### Toast

```js
return new Button("Show toast").addClass("primary").onClick(() => {
    new Toast("Changes saved", "success", { duration: 2500 });
  });
```

<div class="doc-live-demo" data-live-demo="Toast/toast" aria-label="Live preview"></div>

[← Back to API index](./index.html)
