<!-- Generated by scripts/generate-docs.mjs. Edit demos.js / registry.json / JSDoc. -->

# FloatingWindow

**Status:** Stable  
**Category:** overlays  
**Import:** `drawui`  
**Emitted class(es):** `FloatingWindow`, `FloatingWindow-header`, `FloatingWindow-content`, `circle`, `pos-btn`, `green`, `red`, `Row`, `centered-vertical`, `justify-between`, `fill-width`, `fill-height`

Free-floating window with docking controls.

_Source: `src/overlays/FloatingWindow.js`_

## Import

```js
import { FloatingWindow } from "drawui";
```

## Examples

### FloatingWindow

```js
return new Button("Open FloatingWindow").addClass("secondary").onClick(() => {
    document.querySelectorAll(".FloatingWindow").forEach((node) => {
      if (node.parentElement === document.body) node.remove();
    });
    const panel = new FloatingWindow({ title: "Overlay", width: "320px" });
    panel.setContent(
      new StackPanel({ isVertical: true })
        .gap("0.5rem")
        .add(new Heading(3, "FloatingWindow"))
        .add(new TextBlock("Free-floating overlay panel")),
    );
    panel.dom.style.height = "auto";
    panel.dom.style.maxHeight = "70vh";
    panel.show();
  });
```

<div class="doc-live-demo" data-live-demo="FloatingWindow/floatingwindow" aria-label="Live preview"></div>

[← Back to API index](./index.html)
