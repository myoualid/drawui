<!-- Generated by scripts/generate-docs.mjs. Edit demos.js / registry.json / JSDoc. -->

# TreeView

**Status:** Stable  
**Category:** collections  
**Import:** `drawui`  
**Emitted class(es):** `dui-tree`, `dui-tree-item`, `dui-tree-toggle`, `dui-tree-label`, `dui-tree-children`, `is-expanded`, `is-selected`

Expandable nested tree for hierarchical data.

Shares the same options shape as NavigationList (`getChildren`, `getLabel`,
`onItemClick`, `renderItem`) so the same `{ name, children }` trees work for both.

_Source: `src/components/lists/TreeView.js`_

## Import

```js
import { TreeView } from "drawui";
```

## Examples

### TreeView

```js
const sampleTree = {
    name: "Project",
    children: [
      {
        name: "Site",
        children: [
          { name: "Building A", children: [{ name: "Level 01" }, { name: "Level 02" }] },
          { name: "Building B" },
        ],
      },
      { name: "Library", children: [{ name: "Materials" }, { name: "Profiles" }] },
    ],
  };
  const tree = new TreeView({
    getLabel: (item) => item.name,
    getChildren: (item) => item.children || [],
    onItemClick: (item) => console.log("selected", item),
  });
  tree.setData(sampleTree);
  return tree.panel;
```

<div class="doc-live-demo" data-live-demo="TreeView/treeview" aria-label="Live preview"></div>

[← Back to API index](./index.html)
