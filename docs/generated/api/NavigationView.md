<!-- Generated by scripts/generate-docs.mjs. Edit demos.js / registry.json / JSDoc. -->

# NavigationView

**Status:** Stable  
**Category:** shell  
**Import:** `drawui`  
**Emitted class(es):** `NavigationView`, `NavigationView-sidebar`, `NavigationView-content`

Sidebar + content navigation scaffold.

## Import

```js
import { NavigationView } from "drawui";
```

## Examples

### NavigationView

```js
const host = new Container().setStyle("height", ["260px"]).setStyle("overflow", ["hidden"]);
  const layout = new NavigationView({
    sidebarWidth: "180px",
    sidebarMinWidth: "140px",
    sidebarMaxWidth: "320px",
    sidebarResizable : true,
  });
  layout.setSidebarTitle("Explorer");
  layout.setSidebarContent(
    new StackPanel({ isVertical: true })
      .gap("0.25rem")
      .add(new TextBlock("Site"))
      .add(new TextBlock("Building")),
  );
  layout.setMainContent(
    new StackPanel({ isVertical: true })
      .gap("0.5rem")
      .add(new Heading(3, "Viewport"))
      .add(new TextBlock("Main content")),
  );
  host.add(layout);
  return host;
```

<div class="doc-live-demo" data-live-demo="NavigationView/navigationview" aria-label="Live preview"></div>

[← Back to API index](./index.html)
